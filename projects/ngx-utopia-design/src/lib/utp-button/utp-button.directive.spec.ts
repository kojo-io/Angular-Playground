import { UtpButtonDirective } from './utp-button.directive';

describe('UtpButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new UtpButtonDirective();
    expect(directive).toBeTruthy();
  });
});
